'use client'; import { track } from '@/lib/analytics'; import { usePackage } from './package-provider'; import type { Experience } from '@/lib/types';
export function AddExperience({experience}:{experience:Experience}){const {addExperience,experiences}=usePackage();const added=experiences.some(item=>item.id===experience.id);function add(){if(added)return;addExperience(experience);track('experience_added',experience.id,{category:experience.category,price:Number(experience.price)})}return <button className={added?'inline-flex items-center justify-center rounded-full bg-amber px-4 py-2 text-sm font-semibold text-forest':'button'} onClick={add}>{added?'Agregada ✓':'Agregar experiencia'}</button>}
