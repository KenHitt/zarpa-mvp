'use client'; import { useState } from 'react';
export function ConfirmBooking({id}:{id:string}){const [done,setDone]=useState(false);const [error,setError]=useState('');async function confirm(){setError('');const r=await fetch(`/api/partner/bookings/${id}/confirm`,{method:'POST'});if(r.ok)setDone(true);else setError('No se pudo actualizar');}if(done)return <span className="text-sm font-semibold text-emerald-700">Confirmada</span>;return <><button onClick={confirm} className="rounded bg-slate-900 px-3 py-1 text-sm text-white">Confirmar pago</button>{error&&<p className="text-xs text-red-600">{error}</p>}</>}
