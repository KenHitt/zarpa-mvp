'use client'; import Link from 'next/link'; import { usePackage } from './package-provider';
export function Header(){const {hotel,experiences}=usePackage();const items=experiences.length+(hotel?1:0);return <header className="sticky top-0 z-20 border-b border-forest/10 bg-[#f7f7f4]/90 backdrop-blur"><nav className="shell flex h-[72px] items-center justify-between"><Link href="/" className="font-display text-3xl font-bold tracking-tight text-forest">Zarpa</Link><div className="flex items-center gap-4 text-sm font-semibold text-forest sm:gap-6"><Link className="hover:text-amber" href="/experiencias">Experiencias</Link><Link className="hidden hover:text-amber sm:block" href="/hoteles">Hospedaje</Link><Link href="/mi-paquete" className="rounded-full border border-forest/15 px-3.5 py-2 transition hover:border-forest">Mi viaje{items?` · ${items}`:''}</Link></div></nav></header>}
