import { getHotels } from '@/lib/data/catalog'; import { HotelCatalog } from '@/components/catalog-filters';
export const revalidate = 60;
export default async function Hotels({searchParams}:{searchParams:{tier?:string}}){const hotels=await getHotels();return <section className="shell py-12 sm:py-16"><p className="eyebrow">Hospedaje en Tingo María</p><h1 className="section-title">Descansa cerca de la aventura</h1><p className="mt-3 max-w-2xl leading-7 text-forest/70">Añade hospedaje a tu ruta cuando lo necesites; las experiencias siguen siendo el centro de tu viaje.</p><div className="mt-9"><HotelCatalog hotels={hotels} initialTier={searchParams.tier}/></div></section>}
